#include <time.h>
#include <stdio.h>
#include <stdlib.h>
#include "init.h"
#include "pearson_items.h"
#include "sys_funcs.h"


//#include "items.h"

int main(){
    //armour_show();
    add_to_inv("Большая хилка", 10);
    open_inventory();
    /*
    char enter;
    srand(time(NULL));
    while(1){
        start_program(&enter);
        if(enter == 'y' || enter == 'Y'){
            start();
        }
        else{
            return 0;
        }
        
    };
    */
}

